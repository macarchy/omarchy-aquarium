#!/bin/bash
# Omarchy theme-set hook: restart the aquarium so it re-reads the new theme's
# colours. Does nothing unless the aquarium is currently running, and "restart"
# leaves the remembered on/off state alone (this is not the user switching it).

command -v omarchy-aquarium-toggle >/dev/null || exit 0
omarchy-aquarium-toggle restart
